{
  "name": "glossly",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix apps/server\" \"npm run dev --prefix apps/web\"",
    "build": "npm run build --prefix apps/server && npm run build --prefix apps/web",
    "lint": "npm run lint --prefix apps/server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
