# Glossly Environment Variables
# Copy this file to apps/server/.env
# Note: the LLM provider/model/endpoint are configured in the app's Settings
# panel (persisted client-side), not here — these vars only affect the proxy.

# Server Configuration
PORT=3000
HOST=localhost

# Request Timeout (milliseconds)
REQUEST_TIMEOUT=10000
